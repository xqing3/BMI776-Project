# This folder contains Code we used for Preprocessing our data

- `misc` contains code from a tutorial we followed for intial data analysis.
- `DataProcessing.ipynb` and `DataProcessing2.ipynb` provided us insights onto the mice data.
- `Annotation.ipynb` is the code where we use cellTypist to annotate our data.